// Feature flags to control experimental features
// These can be toggled through environment variables or directly here

const featureFlags = {
  // Knowledge Graph feature
  enableKnowledgeGraph: true,
};

export default featureFlags; 